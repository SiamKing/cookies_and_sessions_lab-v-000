<% @cart.each do |item| %>
    <li><%= item %></li>
<% end %>

<h3>Add Item to Cart</h3>
<%= form_tag products_path do %>
    <label>Product</label>
    <%= text_field_tag :product %>

    <%= submit_tag "add to cart" %>
<% end %>